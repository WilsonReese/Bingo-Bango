<h1>Theaters</h1>

<div>
  <h1>Don't want to delete this yet.</h1>
</div>

<div id="theaters">
  <% @theaters.each do |theater| %>
    <%= render theater %>
    <p>
      <%= link_to "Show this theater", theater %>
    </p>
  <% end %>
</div>

<%= link_to "New theater", new_theater_path %>
